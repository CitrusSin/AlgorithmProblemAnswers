#include <iostream>

using namespace std;

int main() {
    int l, n, m;
    cin >> l >> n >> m;

    int stones[n];
    for (int i=0; i<n; i++) {
        cin >> stones[i];
    }
    
    return 0;
}